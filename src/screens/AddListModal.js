import React from 'react'
import { View, StatusBar, StyleSheet, Text, TouchableOpacity, FlatList, Modal, Button } from 'react-native'

function AddListModal() {
    return (
        <>
        
            <Text style={styles.container}>Screen</Text>
    
        </>
    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#fff",
        alignItems: "center",
        justifyContent: "center"
    }
})

export default AddListModal